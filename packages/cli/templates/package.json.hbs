{
  "name": "docs-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "gatsby develop",
    "build": "gatsby build"
  },
  "dependencies": {
    {{#each "dependencies"}}
      "{{@key}}": "^{{this}}"{{#unless @last}},{{/unless}}
    {{/each}}
  }
}
